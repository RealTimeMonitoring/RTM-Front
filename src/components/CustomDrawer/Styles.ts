import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
      flex: 1,
    },
    scrollView: {
      flexGrow: 1,
    },
    logoutContainer: {
      padding: 10,
      borderTopWidth: 1,
      borderTopColor: "#ccc",
      backgroundColor: "#f9f9f9",
    },
  });