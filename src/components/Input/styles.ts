import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{
        width: "100%"
    },

    group:{
        width: "100%",
        height: 50,
        backgroundColor: "#FFF",
        justifyContent: "center",
        overflow: "hidden"
    },

    field:{
        paddingLeft: 10,
        height: 50,
    }
})